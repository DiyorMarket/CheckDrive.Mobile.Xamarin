<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="CheckDrive.Mobile.Views.HistoryPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:md="clr-namespace:Plugin.MaterialDesignControls.Material3;assembly=Plugin.MaterialDesignControls">
    <ContentPage.Content>

        <StackLayout Margin="5">
            <md:MaterialLabel
                Grid.Row="0"
                Margin="0,15,0,0"
                FontSize="Medium"
                HorizontalOptions="Center"
                Text="Tarix"                
                TextColor="Black" />

            <CollectionView x:Name="HistoryList" SelectionMode="None" ItemsSource="{Binding DispatcherReviews}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <Grid Grid.Row="0" Margin="0,25,0,0">
                                <Button
                                    Margin="25,25,0,0"
                                    BackgroundColor="#F2F2F2"
                                    CornerRadius="15"
                                    FontSize="Medium"
                                    HorizontalOptions="Start"
                                    Text="Pay, 9 May"
                                    TextColor="Black" />
                                <Button
                                    Margin="0,25,25,0"
                                    BackgroundColor="#F2F2F2"
                                    CornerRadius="15"
                                    FontSize="Medium"
                                    HorizontalOptions="End"
                                    Text="315 litr"
                                    TextColor="Black" />
                            </Grid>

                            <!--  Right Column  -->
                            <StackLayout>
                                <Image
                                    HeightRequest="30"
                                    Source="icon_check.png"
                                    WidthRequest="30" />
                                <!--  Add similar Image tags for each status picture  -->
                            </StackLayout>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>

